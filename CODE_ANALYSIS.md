# Code analysis progress

Started 2026-09-23. Review order: root composition crate, then feature crates, shared Bevy crates, and Bevy-free crates, following `contributing/src/overview.md` and `contributing/src/module-dependency-rules.md`. Preserve public APIs unless a larger architectural benefit is demonstrated. Existing user changes in `Cargo.toml`, `Cargo.lock`, `crates/harmonicon-audio/src/pipeline.rs`, `CHANGELOG.md`, and packaging files are outside this review and must be preserved.

## Completed files

- `src/main.rs` — thin entry point; no change needed.
- `src/lib.rs` — plugin assembly and startup ordering checked against the contributor architecture guide; removed a stale Bevy 0.19 RC comment. No hot loop here.
- `src/dev_capture.rs` — dev-only screen capture; `Image` cloning is required by the consuming conversion API. No change needed.
- `src/bin/gen_synthetic_dataset.rs` — thin I/O wrapper; no change needed.
- `src/bin/note_bench.rs` — sorting recording directories requires materialization; no significant copy or hot-loop issue in this wrapper.
- `src/bin/hole_editor.rs` — skip mesh, material, and status text updates while editor state is unchanged; this avoids repeated per-frame string formatting and asset mutation. No public API.
- `src/bin/note_editor.rs` — avoid marking editor state changed every frame when the resize key is unchanged; format status text only on actual state changes. Existing preview updates already use change detection. No public API.
- `crates/harmonicon-dsp/src/lib.rs` — removed the per-frame harmonic suppression bitmap and the NMF activation loop's 50 temporary denominator vectors per analysis block. Public API unchanged. `cargo test -p harmonicon-dsp` passed (31 tests).
- `contributing/src/overview.md` — corrected the Bevy version, desktop entry-point description, and inaccurate blanket claim about re-export facades.
- `crates/harmonicon-lessons/src/lib.rs` — plugin registration and schedule order match the documented lesson/menu boundary; no change needed.
- `crates/harmonicon-lessons/src/lesson_tree/layout.rs` — removed per-node temporary vectors from barycentre calculation and reused layer storage during ordering sweeps. Layout behavior unchanged; all 76 lessons tests passed.
- `crates/harmonicon-lessons/src/lesson_tree/edges.rs` — matched the documented material-sharing behavior by reusing solid-edge handles per style and diagonal within a tree build, instead of allocating one asset per edge. `lesson_tree/mod.rs` owns the small local cache; its broader review is still pending. All 76 lessons tests passed.
- `crates/harmonicon-lessons/src/lesson_tree/transition.rs` — skip idle slide updates, repeated viewport resource writes, and per-frame chevron string allocation when the label is unchanged. All 76 lessons tests passed.
- `crates/harmonicon-lessons/src/lesson_tree/mod.rs` — removed a cloned unit-ID vector during tree setup and the temporary localized-title vector for locked-node tooltips. Existing ECS-owned IDs and image handles still require cloning. All 76 lessons tests passed.
- `crates/harmonicon-lessons/src/lesson_reader/mod.rs` — page assembly and widget constructors reviewed. Initialize the displayed BPM cache for each metronome and phrase looper. The remaining manifest clones belong to owned Bevy components or button observers. All 76 lessons tests and Clippy passed.
- `crates/harmonicon-lessons/src/lesson_reader/widgets.rs` — eliminated per-frame BPM label formatting and text replacement while the tempo is unchanged. All 76 lessons tests and Clippy passed.
- `crates/harmonicon-menu/src/lib.rs` — replaced malformed and inaccurate crate documentation; retained the existing public re-export API.
- `crates/harmonicon-menu/src/menu/mod.rs` — plugin registration and page cleanup ordering reviewed; no change needed.
- `crates/harmonicon-menu/src/menu/routing.rs` — routing and Escape navigation reviewed; corrected a stale reference to the removed lesson list page.
- `crates/harmonicon-menu/src/menu/scene.rs` — shared page and button helpers reviewed; removed documentation that described an older themed-button implementation and clarified the root marker's actual role.
- `crates/harmonicon-menu/src/menu/pages/artist_list.rs` — artist sorting already uses borrowed names; observer clones an owned artist name only when selecting. No change needed.
- `crates/harmonicon-menu/src/menu/pages/song_list.rs` — sort borrowed song entries instead of cloning the complete song list before spawning buttons. The observer still owns its asset path. All 36 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/mod.rs`, `main_menu.rs`, `play.rs`, `mode_select.rs`, and `jam_session.rs` — declarative page setup and routing only; no material allocation or copy issue found.
- `crates/harmonicon-menu/src/menu/pages/help_about.rs` — local documentation discovery and page setup reviewed; no change needed.
- `crates/harmonicon-menu/src/menu/pages/welcome.rs` — removed a stale Bevy 0.19 comment and moved the profile-save explanation onto the function it describes.
- `crates/harmonicon-menu/src/menu/pages/options/sliders.rs` — the latency slider already used `AudioSettings` change detection; made the volume slider sync use the same guard so idle frames do not rewrite fills or format percentage labels. All 36 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/options/microphone.rs` — replaced the hard-coded Retry button label with the existing localized retry key. Status and combobox sync already use change detection. All 36 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/options/zoom.rs` — corrected documentation that referred to a removed keyboard shortcut and implied continuous updates; the code already filters changed slider values.
- `crates/harmonicon-menu/src/menu/pages/jam_generate.rs` — comboboxes require owned label vectors; generated song setup and resource handoff reviewed. Corrected the description of the backing as looping, since Jam Session can schedule an ending.
- `crates/harmonicon-menu/src/menu/pages/theme_picker.rs` — theme names are owned by button components and observers; preview handles and button visuals already update only when selection changes. No change needed.
- `crates/harmonicon-menu/src/menu/pages/tutorial.rs` — the tour timer mutates its resource every frame, so `is_changed()` rebuilt the overlay every frame despite the comment claiming step-only updates. Track the last shown step locally, remove a redundant target clone, and cover timer-tick stability with an ECS test. All 37 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/credits.rs` — reuse the traversal stack while propagating render layers to late glTF children instead of allocating a new vector every frame. Clarified the root marker documentation. All 37 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/calibration.rs` — beat dots stop receiving writes when calibration is inactive and unchanged; the offsets summary rebuilds only when the number of recorded hits changes, directly into one `String` instead of a temporary vector of strings. All 37 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/options/mod.rs` — Options setup and plugin systems reviewed; pass borrowed harmonica model names through the temporary preview list, leaving ownership only where the button scene needs it. All 37 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/options/harmonica.rs` — reuse the glTF child traversal stack across frames instead of allocating a new vector every update, and accept borrowed names from Options setup. All 37 menu tests passed.
- `crates/harmonicon-menu/src/menu/pages/harp_check.rs` — pass event modifiers and the loaded chart by reference during remap-cost calculation; borrow current combobox labels; use the selected track index instead of rebuilding localized labels to identify it. All 37 menu tests passed.
- `crates/harmonicon-editor/src/lib.rs` — fixed malformed crate documentation containing literal newline escapes and removed a misleading registration claim; the public re-export API is unchanged.
- `crates/harmonicon-editor/src/song_editor/mod.rs` — reviewed plugin registration and schedule gates; removed stale comments claiming gameplay uses the editor's private playback module and that tick constants still live in `audio_system`. No behavior or API change.
- `crates/harmonicon-editor/src/song_editor/state.rs` — build the playback meter map from borrowed signatures, avoiding the intermediate owned `TimeSigPoint` vector and its string clones. Keep the existing earliest-duplicate behavior. The broad `effective_harp()` clone remains because mutation-oriented callers need an owned harp; a borrowed return would require coordinated call-site changes. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/note_model.rs` — value types and constructors have no meaningful copy or allocation problem; removed historical split rationale and corrected stale technique and synth documentation.
- `crates/harmonicon-editor/src/song_editor/grid_cache.rs` — update existing snapshots with `clone_from` so repeated grid rebuilds reuse vector and string capacity instead of replacing every allocation. First-build and invalidation behavior stay the same; both cache tests passed.
- `crates/harmonicon-editor/src/song_editor/grid.rs` — borrow the selection IDs at group-drag start and the drag state on each pointer move, eliminating the per-move clone of the selected-note group. Target vectors are still needed by the group-validity API, and Bevy-owned grid entities still require materialization. The nine grid-filtered editor tests passed.
- `crates/harmonicon-editor/src/song_editor/interaction.rs` — borrow drag state during resize and avoid unconditional grip component writes on idle frames, reducing cloning and Bevy change-detection churn. The sticky intensity string still needs ownership in note metadata. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/clipboard.rs` — reserve paste-target capacity once from the clipboard length and fix the stale function link in module docs. Returned owned notes remain required for assigning fresh IDs. All six paste tests passed.
- `crates/harmonicon-editor/src/song_editor/metadata_sync.rs` — reuse the editor's selection vector when opening a phrase or pasting, avoiding temporary or replacement ID vectors. Other metadata clones are required to retain copied clipboard values or re-key annotations. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/undo.rs` — use `VecDeque` for history so evicting old snapshots does not shift the retained history; correct docs that omitted already-tracked metadata. Snapshot clones on undo/redo still serve the current state and history independently. All 12 undo-filtered tests passed.
- `crates/harmonicon-editor/src/song_editor/ranges.rs` — calculate silence gaps in one scan of sorted note intervals, avoiding the intermediate merged-interval vector. Erase/remove retain their existing owned-vector API for callers; removed historical split rationale from module docs. All six silence-filtered tests passed.
- `crates/harmonicon-editor/src/song_editor/timeline.rs` — interaction handlers already avoid temporary vectors and gate persistent surface writes; corrected module opening to describe all tool modes it handles. Click-time formatting is not a hot path.
- `crates/harmonicon-editor/src/song_editor/timeline_overlay.rs` — guard split-line and highlight component writes so idle frames do not mark Bevy UI components changed. Overlay geometry and visibility remain derived from the same state. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/view_scroll.rs` — remove per-frame temporary vectors for touch deltas and child heights, and guard idle grid, root, and scrollbar node writes. Scrollbar marker entities still need an owned ID list for Bevy parenting. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/scroll.rs` — skip unchanged vertical scrollbar visibility writes on idle frames. Form construction is one-time and its owned components are necessary. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/ui.rs` — avoid respawning the hole column for unrelated editor state changes, while still rebuilding for a new column entity, hole count, theme, or localization; guard unchanged chrome heights. Initial setup remains declarative. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/panel.rs` — guard mode-group, note-column, and technique-button display writes when editor state changes but these modes remain the same. The other panel labels and status messages still update from their existing state/theme gates. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/panel_widgets.rs` — one-time button construction and observers reviewed; the label clone is needed because text and the live-label marker both own the base string. No code change needed.
- `crates/harmonicon-editor/src/song_editor/mod_panel.rs` — one-time sidebar assembly has no meaningful clone or hot-loop issue; removed obsolete two-strip documentation and fixed a misplaced function description.
- `crates/harmonicon-editor/src/song_editor/transport.rs` — file dialog vectors are owned message fields created on user clicks; transport button setup has no hot-loop allocation issue. Removed historical module-split rationale and inaccurate Import claim.
- `crates/harmonicon-editor/src/song_editor/playback.rs` — guard unchanged playhead and progress UI writes, especially while playback is idle; correct preset-harp documentation. The renderer's `PhraseNote` slice still requires a vector at playback start. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/practice.rs` — borrow the latest pitch event's data instead of collecting a temporary frequency vector every frame; defer waiting-message strings until one is actually needed. Schedule storage remains owned for the duration of a practice session. All 18 practice-filtered tests passed.
- `crates/harmonicon-editor/src/song_editor/record.rs` — reuse a local raw-MIDI vector across detector events and remove temporary span vectors while growing or closing notes. The tracker still returns its own owned active-pitch vector; that API is in the shared core crate. All 21 recording-filtered tests passed.
- `crates/harmonicon-editor/src/song_editor/metronome.rs` — cache the parsed meter map across playback frames, rebuilding only when the opening signature or meter-change list changes; avoid unchanged shared tempo-resource writes. All 12 metronome tests passed.
- `crates/harmonicon-editor/src/song_editor/audition.rs` — avoid unchanged `LastAuditioned` writes when selection is empty and correct the stale synth-module description. Audio bytes must be owned by the Bevy asset. Both audition tests passed.
- `crates/harmonicon-editor/src/song_editor/waveform.rs` — reject unsupported extensions before reading the file and compare extensions case-insensitively without allocating a lowercase string; correct the decoder-module description. Worker task and path ownership remain necessary. All 10 waveform tests passed.
- `crates/harmonicon-editor/src/song_editor/midi_import.rs` — detect duplicate holes without allocating a set per chord; move the opening time-signature string out of the imported meter vector instead of cloning all meter strings; use the combobox's selected index instead of formatting each track label to find it; correct a stale synth reference. All 24 MIDI-filtered tests passed.
- `crates/harmonicon-editor/src/song_editor/pitch_map.rs` — the adapter preserves core's resolution and only materializes alternatives where callers need owned choices; trimmed historical resolver commentary. Key scoring still constructs each candidate preset by necessity.
- `crates/harmonicon-editor/src/song_editor/transpose.rs` — resolve candidate positions on demand during collision passes instead of allocating a full placed-note vector each pass; borrow selected IDs instead of cloning them. Candidate alternatives still need owned storage while the solver tries placements. All seven transpose tests passed.
- `crates/harmonicon-editor/src/song_editor/music_score_bridge.rs` — emit tied notation segments directly, avoiding a temporary boundary vector; share the metronome's cached meter map for playhead updates and guard unchanged score resource writes. Trimmed historical bridge documentation. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/metronome.rs` (revisited) — expose its existing meter-map cache lookup to the score bridge so both systems use the same invalidation logic. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/annotation_lane.rs` — scan visible phrase markers with a peekable iterator instead of collecting a temporary entry vector, and compute each marker label once. The label's owned string is still needed by the UI text and tooltip. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/details_fields.rs` — fixed enum and static field-label registry have no runtime allocation, temporary vector, or copy issue. No code change needed.
- `crates/harmonicon-editor/src/song_editor/expected_notes.rs` — borrow drag state and selected note instead of cloning on pointer moves or panel updates; only build a harp for the bend modifier; avoid unchanged visibility and color writes. The dev-only overlay still rebuilds from scratch on editor-state changes, intentionally for short benchmark clips. All 377 editor tests passed with `dev` enabled.
- `crates/harmonicon-editor/src/song_editor/debug_record.rs` — guard unchanged waveform row, bar height, checkmark, and status-label writes to avoid Bevy change-detection churn. Recording output and capture lifecycle stay the same; bucketing still returns a vector through the shared API. All 377 editor tests passed with `dev` enabled.
- `crates/harmonicon-editor/src/song_editor/harpchart.rs` — serialize harmonica layouts from borrowed slices instead of cloning each vector, use empty slices while parsing absent event/modifier arrays, and sanitize path segments in one pass without temporary collections. JSON values and returned chart strings remain owned by the serialization API. All 377 editor tests passed with `dev` enabled.
- `crates/harmonicon-editor/src/song_editor/legend.rs` — skip unchanged visibility component writes and remove historical file-split commentary. One-time localized legend construction has no relevant hot-path allocations. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/lesson_form.rs` — guard unchanged lesson panel, body, and conditional-row display writes; borrow cached header labels and use `clone_from` only when the text changes. Manifest-owned strings remain necessary in editor state. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/loop_settings.rs` — four UI-editable owned strings and a static loop-type array; no temporary vectors or hot-path copies. No code change needed.
- `crates/harmonicon-editor/src/song_editor/material.rs` — Bevy material descriptor and plugin registration only; shader uniforms are compact copied values and there is no host-side loop to vectorize. No code change needed.
- `crates/harmonicon-editor/src/song_editor/meta_form.rs` — reuse existing string allocations when committing text fields and syncing meter combobox values; trim overlong module description. Combobox option vectors and file-dialog extension vectors are owned by their widget/message APIs and constructed only on setup or click. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/phrase_editor.rs` — reuse the editor's cached meter map while the popover is open, guard unchanged popover layout writes, and reuse title string capacity. Text box commits still need an owned annotation string. All 377 editor tests passed.
- `crates/harmonicon-editor/src/song_editor/save_feedback.rs` — short-lived localized status message and countdown perform no temporary allocations or unnecessary copies; the timer update while active is needed to expire the message. No code change needed.
- `crates/harmonicon-editor/src/song_editor/scoring_settings.rs` — UI-editable scoring strings are necessarily owned in the editor state; this value type has no hot loop or temporary vector. No code change needed.
- `crates/harmonicon-editor/src/song_editor/selected_metadata.rs` — metadata accessors return borrowed strings; setters retain owned annotations or intensity text as required by editor state. The few depth labels are built only on user interaction. No code change needed.
- `crates/harmonicon-editor/src/song_editor/tests.rs` — test fixtures and assertions are off the runtime path. Existing coverage exercises chart round trips, editor interactions, lesson manifests, and safe path segments; no production allocation or SIMD change belongs in this file. No code change needed.
- `crates/harmonicon-editor/src/song_editor/transpose/tests.rs` — transpose fixtures and assertions are outside the runtime path; their temporary vectors make expected note positions easy to compare. All seven transpose tests passed in the editor suite; no code change needed.
- `crates/harmonicon-jam/src/lib.rs` — crate root only exports the Jam module; corrected literal `\\n` escapes and overlong crate documentation. No runtime change.
- `crates/harmonicon-jam/src/jam/mod.rs` — plugin registration establishes explicit order for the shared gameplay schedule; no runtime allocation or copy issue in this file. Removed stale `main.rs`/dependency-cycle history from module documentation.
- `crates/harmonicon-jam/src/jam/session.rs` — reuse stem-mute storage on session setup and skip unchanged loop-label text writes. Chart note markers and chord sequence remain owned by their UI/resources; audio asset handles are cheap reference-counted clones needed by new players. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/session_ui.rs` — guard unchanged guide display, visibility, width, and label writes, and format chord-position text once per bar update. Static twelve-bar strip construction has no hot-path allocations. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/hole_map.rs` — replace the per-frame lit-hole hash map with a fixed array indexed by `u8` hole ID, reserve setup-time hole info capacity, and skip unchanged background writes. The persistent pitch-to-holes and chord-class maps remain appropriate lookup structures. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/improv.rs` — replace the per-frame set of sounding `u8` MIDI pitches with a fixed presence array before releasing inactive attack-gate entries. Persistent scale/chord hash sets are borrowed. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/lesson.rs` — on-demand lesson judging has no hot-path allocation or meaningful reference change; the lesson ID clone is needed as an owned profile key. Trimmed historical module documentation. No runtime change.
- `crates/harmonicon-jam/src/jam/midi_tracks.rs` — skip unchanged stem-mute icon and background writes, avoiding per-frame text allocations and UI change-detection churn. Audio gain is still applied each frame to preserve ordering after the global volume system; that path has no temporary vector. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/rhythm_guide.rs` — guard unchanged pulse-row background and border writes, particularly the seven idle slots on each frame. The active slot still animates every frame; no temporary vector is built. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/position_guide.rs` — borrow the compass slot's harp key while rebuilding its caption instead of cloning the string on each position call. Scale-class recomputation remains necessary when a lesson changes position. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/call_response.rs` — reuse ghost-hole vector capacity for each new call, and guard unchanged banner, toggle, and density text/visibility writes. The owned `PhraseNote` list is required by the synth rendering API, and calls happen only at bar boundaries. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/call_response/phrase.rs` — reserve the playable-note vector from the harp's hole count and choose weighted motif candidates directly from their `(note, weight)` pairs, removing the extra weight vector. Kept the `lay_out` vector return shape used by focused tests. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/backing.rs` — keep at most three comping frequencies on the stack and scale each rendered slot's samples in place instead of collecting another vector. Generated PCM stems, WAV bytes, and the mixed waveform buffer are owned by their output APIs; changing per-sample math for SIMD needs audio comparison benchmarks. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/backing/humanize.rs` — shift and scale each varied slot inside the rendered sound's own buffer instead of allocating a second slot-length vector; clamp the delay to the slot so an oversized delay still yields a silent slot of the original length. All 122 Jam tests passed.
- `crates/harmonicon-jam/src/jam/backing/preview.rs` — one-shot dev-tooling mix for the listening example; no hot path. No change needed.
- `crates/harmonicon-jam/src/jam/band.rs` — a frame spanning more than one beat fed the listener only the completed beat, breaking its every-beat contract and able to skip a phrase boundary (comping target) or a due answer. `BandListener::observe_until` now feeds skipped beats as silent, capped to the log length for long stalls; three tests cover it. Phrase density reads the log through `make_contiguous` instead of collecting a vector. All 125 Jam tests and Clippy passed.
- `crates/harmonicon-jam/examples/listening_matrix.rs` — one-shot export of nine previews; no change needed.
- `crates/harmonicon-jam/src/jam/{band,call_response,call_response/phrase,hole_map,improv,session}/tests.rs` — fixtures and assertions are off the runtime path; no change needed.
- `crates/harmonicon-bench/src/note_bench.rs` — `compare` moved each frame's expected set into the confusion key instead of cloning it, and broke count ties by pitch set: `HashMap` order made the printed confusion table differ between runs of a benchmark meant to be reproducible. Corrected pre-split module paths in docs. Per-frame `expected_at` is a linear scan; it is fine for recording-sized charts. All 22 bench tests passed.
- `crates/harmonicon-bench/src/synthetic_dataset.rs` — one-shot generator; no hot path. Corrected pre-split module paths in docs. Root `CLAUDE.md`'s crate table wrongly listed this crate as using Bevy; fixed.
- `crates/harmonicon-gameplay/src/lib.rs`, `gameplay/mod.rs`, `gameplay/plugin.rs` — wiring only; schedule ordering matches the crate notes. Removed a truncated duplicate bullet and a stale synth path from `crates/harmonicon-gameplay/CLAUDE.md`.
- `crates/harmonicon-gameplay/src/gameplay/clock.rs` — writes only on transitions; anchoring is pure and tested. No change needed.
- `crates/harmonicon-gameplay/src/gameplay/judge.rs` — `score_notes` rebuilt a `HashSet` of sounding pitches and a pending-index `Vec` every frame, and `observed_failure` collected an `attacked` vector per pending note. Both buffers now live in a reused `Local<JudgeScratch>`, `nearest_attacked` takes an iterator (the wait-freeze overlay no longer collects either), and a frame's RMS is computed once instead of once per sustaining wah note. The hit-time modifier vectors remain; they only allocate on a judgment. All 424 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/notes.rs` — `notes_needing_spawn` returned a fresh `Vec` every frame and both highways collected every spawned note ID into a new `HashSet` to feed it. It now yields a lazy iterator, and `spawn_visible_notes`/`spawn_visible_notes_3d` refill a reused `Local` set. `build_scheduled_notes` runs only at setup or on an adaptive-difficulty resync; its clones are not on the frame path. All 424 gameplay tests passed.
- `crates/harmonicon-gameplay/src/gameplay/state.rs` — `collect_pitches` replaced `ActivePitches` with a new vector on every detector event, cloning each `PitchInfo` (which owns a `String`) even though only the frame's last event survives. Unfiltered detectors now `clone_from` only the last event; the harmonica filter still feeds every hop to its tracker but reuses a `midis` buffer and writes straight into `ActivePitches`. `HarmonicaNoteTracker::update` in core still allocates its own sets and vectors per hop; that belongs to the core review. All 424 gameplay tests passed.
- `crates/harmonicon-gameplay/src/gameplay/gameplay_2d.rs`, `gameplay_3d/notes.rs`, `gameplay_3d/scene.rs` — in the locked Bevy (`v0.20.0-rc.1`, git `1b1f3ec`), `Assets::get_mut` returns an `AssetMut` whose `DerefMut` queues `AssetEvent::Modified` on drop, so any field assignment re-extracts and re-uploads the material even when the value is identical. `update_note_visuals(_3d)` did that for every visible note's head and tail material every frame, and `update_holes_3d` for every hole. All three now compare via `get` and only `get_mut` on a real difference. This fully removes the per-frame 3D head and hole uploads; tail materials are still re-uploaded each frame by `animate_note_tails(_3d)` (see follow-up below), so for tails the guard only drops a redundant second write outside reduced motion; 2D head tints and hole `BackgroundColor`s are guarded the same way. `step_hole_glow`'s exponential ease never reached its target (about 25 s of float underflow per idle cell), so it now settles once within 1e-3 — far below one 8-bit colour step — with a test. `harp_pitches` refills a per-system `Local` set instead of allocating one each frame in four systems. All 425 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/gameplay_2d.rs` (rest) — `size_note_tails` rewrote every tail's height each frame; now only on a highway resize. `setup` moved the head image path and tail config into `NoteRenderAssets` instead of cloning them. `update_notes` must move every note each frame and `animate_judged_notes` already writes only on a transition. All 425 gameplay tests passed.
- `crates/harmonicon-gameplay/src/gameplay/note_tail_2d.rs`, `note_tail_3d.rs` — material definitions and pure `tail_params`; per-frame tail uploads recorded as a follow-up.
- `crates/harmonicon-gameplay/src/gameplay/gameplay_3d/notes.rs`, `gameplay_3d/scene.rs` — spawn, scroll, label and judged-note systems already write only what moves or transitions. `groove_harmonica` ignored `ReducedMotion`, the one decorative motion left running with it on; it now holds the model still, writing only when the pose changes, and the setting's doc, the player guide (`options.md`) and `gameplay_validation.md` list it. Each 3D note still gets its own head `StandardMaterial` and tail mesh; sharing per-state handles would be a larger change. All 425 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/hud.rs` — judgment labels were already message-driven, but the fade wrote both text colours, `HitFeedback::timer` and `judgment = None` every frame even with nothing on screen; each is now written only on a change.
- `crates/harmonicon-gameplay/src/gameplay/beat_guides.rs` — the pool must move every frame, but visibility (which re-runs propagation), height and colour are now written only on a change.
- `crates/harmonicon-gameplay/src/gameplay/bars.rs` — `track_current_bar` rewrote `CurrentBar`/`AbsoluteBar` every frame, so Jam Session's `session_ui` label update and `session`'s ending check, both gated on `is_changed()` to mean "the bar moved", ran every frame. Both resources are now written only when the bar changes. `beat_ticks_in_range` returns an iterator instead of a per-frame `Vec`. All 425 gameplay and 125 Jam tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/music_score_bridge.rs` — re-implemented the chart-meter precedence inline despite the crate's rule that `bars::chart_meter` is the one reading; it now calls `chart_meter`. The playhead resource was rewritten every frame, including while paused; now only when it moves.
- `crates/harmonicon-gameplay/src/gameplay/twelve_bar_blues_overlay.rs`, `song_waveform_material.rs`, `hud_panel.rs`, `note_visual_2d.rs` — already message-gated or one-time setup; clones are reference-counted handles and paths. No change needed.
- `crates/harmonicon-gameplay/src/gameplay/countdown_overlay.rs` — after the countdown, `update_countdown` wrote `Visibility::Hidden` to the overlay every frame for the whole song, re-running visibility propagation over its subtree; it now writes only on the flip, and the digit is reformatted once a second rather than every frame. Corrected a stale "0.19-rc.3 `bsn!` font" comment — the children use the default font.
- `crates/harmonicon-gameplay/src/gameplay/song_info.rs` — the key/tempo/meter line read `song.time_signature` alone, so a chart whose `time_signature_map` overrides it showed a meter different from the one the bar counter and metronome use. It now goes through `chart_meter` via a tested `time_signature_label`. Trimmed history narration from its docs.
- `crates/harmonicon-gameplay/src/gameplay/practice_badges.rs` — updates only when a practice aid changes. No change needed. All 426 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/lifecycle.rs`, `note_feedback.rs`, `highway_2d.rs`, `warning_banner.rs` — setup/teardown, pure functions, spawners, or already change-gated. No change needed.
- `crates/harmonicon-gameplay/src/gameplay/modifier_legend.rs`, `metronome_overlay.rs` — `update_technique_legend_visibility` (every frame, every app state), `update_mute_label`, `update_feel_label` and `update_tempo_label` rewrote their text (a fresh localized `String` each frame, forcing text re-layout) and the legend's `Node::display` every frame, justified as keeping a newly spawned label from going stale. They now write when the setting or locale changes or when the label's marker `is_added()` — which covers a fresh label directly. An ECS test pins the three cases for the mute label. All 427 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/phrase_overlay.rs` — message-driven; strings are built only at phrase boundaries. No change needed.
- `crates/harmonicon-gameplay/src/gameplay/song_progress_overlay.rs` — `sync_note_marker_colors` wrote `BackgroundColor` to every note marker every frame, justified as "a few dozen visible"; the bar actually holds one marker per note in the whole song. It now paints all markers on a theme/palette change and otherwise only newly added ones. The playhead is written only when it moves. Trimmed history narration from the mastery-bar docs. Loop marker, mastery bars, overlay visibility and phrase borders were already change-gated. All 427 gameplay and 125 Jam tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/metronome_overlay.rs` (rest) — `update_metronome` rewrote every beat dot's colour each frame although only the current beat animates; idle dots are now left alone. Click playback spawns only on a new tick.
- `crates/harmonicon-gameplay/src/gameplay/harmonica_overlay.rs` — `update_harmonica_overlay` (Jam Session and the Bending Trainer) collected the sounding pitches into a `HashSet` and rewrote every cell's colour each frame; it now scans the few active pitches directly and writes only changed cells. All 427 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/drill.rs` + `harmonica_overlay.rs` — in the trainer, the live-highlight overlay and `update_drill_progress_tint` both wrote every diagram cell each frame, the first resetting what the second then re-tinted, so no guard could stick. Each cell now has one writer: the overlay skips `DiagramCellTarget` cells, and the tint system paints them fully (lit, accuracy tint, or default) only on a change, without a per-frame `HashSet`. Colours are identical to before. All 427 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/drill.rs` (drill loop) — while a drill ran, `drill_update` wrote its per-attempt timers (`elapsed_secs`, `hold_secs`, `attempted`) through `ResMut` every frame, so `DrillState::is_changed()` was always true and the drill label, scope label, button colour, target-progress line and `update_drill_slots` (two `Drawer::open` writes) all rebuilt every frame. Nothing displays those timers, so they now tick through `bypass_change_detection()`; `finish_attempt` still marks the change when an attempt ends. A new ECS test fails on the old code and passes now. All 428 gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/adaptive_difficulty.rs` — reserve unlock flags from the known event count and combine the two temporary per-section boolean vectors into one `Option<bool>` vector while preserving unseen, clean, and failed sections. Note-state transfer still matches by time/hole/direction and clones retained sample histories by necessity. All 428 dev-feature gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/autoplay.rs` — keep the dev-only public `sounding` result API, but reuse `ActivePitches` and audio-frame sample vector capacity each frame instead of replacing and clearing them. The six autoplay tests passed with `dev` enabled.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/advanced.rs` — update pitch readouts only when their input resources or marker entities change, and reuse label string capacity when text changes; remove overlong module rationale. Live trace changes still update the readout. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/diagram.rs` — preserve the existing drill-change/new-bar gate and skip identical progress-bar width and visibility writes within it. Keyboard navigation and bar attachment have no temporary collection. All 428 dev gameplay tests passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/feedback.rs` — replace the per-update heap-allocated MIDI family set with a fixed presence table, compute target log-frequency once for nearest-pitch comparisons, localize the natural-check label once per update, and skip unchanged tuner text/color writes. The small readiness sample average does not warrant SIMD. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/gesture.rs` — advance private gesture timers without marking the Bevy resource changed; notify observers only when the displayed phase changes. Rebuild the localized shape label once per actual state/locale update and skip identical text writes. The gesture state machine uses scalar comparisons with no useful SIMD work. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/layout.rs` — update setup/progress labels when locale changes, build each localized value once per update, reuse text buffers, and avoid unchanged drawer writes. Trimmed obsolete layout rationale from module docs. UI construction is setup-only and has no useful SIMD path. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/mod.rs` — derive the detector range from `TrainerKey`'s existing harp instead of building another, avoid marking the key changed on an identical selection, update diagram borders only on target/scope/new-cell changes, and update target/hint labels on locale changes without identical text writes. Removed stale pre-layout module documentation. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/tests.rs` — reviewed the trainer's classification, gesture, drill, calibration, layout, and navigation coverage. Fixtures and expected-value vectors are outside the runtime path; no SIMD or allocation change belongs in this test module. No code change needed.
- `crates/harmonicon-gameplay/src/gameplay/bending_trainer/trace.rs` — calculate recent-window line-fit residuals over a filtered iterator instead of collecting a `VecDeque`, reuse a per-system smoothing buffer, and hold the 32 visible rail samples in a stack array instead of a per-frame vector. The short history and sequential smoothing are poor SIMD targets. Rail labels and positions still receive identical writes each frame; a separate change should gate those without obscuring the rail's query structure. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/call_response.rs` — borrow authored pitch names and modifier slices when building call demos instead of cloning them, and reserve the known phrase-note count. The cue vector and synthesized PCM are required owned output built once per chart; there is no useful SIMD work in phrase setup. Trimmed obsolete module narrative. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/coaching.rs` — pure results-screen analysis over small fixed technique buckets and borrowed missed-note slices. `ranked_techniques` returns its required owned output, while missed-range search uses partition points without temporary collections. No useful SIMD or reference change; no code change needed.
- `crates/harmonicon-gameplay/src/gameplay/gameplay_3d/mod.rs` — reviewed 3D setup, resource bundles, fallback hole layout, and HUD construction. The note-marker vector is required by song-progress spawn and built once per song; model holes are cloned once because both rendering assets and the model spawn own them. No per-frame temporary vector or useful SIMD path in this file. No code change needed.
- `crates/harmonicon-gameplay/src/gameplay/gameplay_3d/tests.rs` — reviewed lane geometry, label scaling, note tint/depth, camera restore, and note expiry assertions. The module is test-only and has no production allocation or SIMD opportunity. No code change needed.
- `crates/harmonicon-gameplay/src/gameplay/pause_menu.rs` — refresh five localized readouts on locale changes, reuse their text allocations, and skip identical slider component/fill/label writes. Removed stale Bevy 0.19 and overly long layout rationale. Pause-menu setup and on-demand profile writes have no SIMD target. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-gameplay/src/gameplay/results.rs` — one-shot results setup, profile update, and UI construction. The missed-note vector is needed by the sorted-slice range search; the small technique-accuracy vector is passed to lesson/profile APIs only on completion. No per-frame allocation or useful SIMD target; no code change needed.
- `crates/harmonicon-gameplay/src/gameplay/tests.rs` — reviewed the gameplay integration coverage for meter, clocks, looping, judging, note spawning, waiting, and schedule initialization. Fixture vectors and assertions run only in tests; no production allocation or SIMD change belongs here. No code change needed.
- `crates/harmonicon-gameplay/src/gameplay/wait_freeze_overlay.rs` — gate target/heard readouts on their actual ECS inputs, refresh localized text on locale changes, reuse text buffers, and skip unchanged visibility writes. Trimmed overlong module rationale. All 428 dev gameplay tests and workspace Clippy passed.
- `crates/harmonicon-menu/src/lib.rs` — crate root preserves the existing public menu export; no runtime work or collection. No code change needed.
- `crates/harmonicon-menu/src/menu/mod.rs` — plugin registration and page lifecycle ordering only, with one state-order test. No per-frame collection, unnecessary copying, or SIMD opportunity in this wiring file. No code change needed.
- `crates/harmonicon-menu/src/menu/routing.rs` — menu state transitions, asset-load gate, return flags, and their tests. No per-frame collection; mutations occur only on navigation or loading transitions. The routing branches are explicit and preserve welcome/tutorial precedence. No code change needed.
- `crates/harmonicon-menu/src/menu/scene.rs` — shared menu chrome and scroll/plain body constructors run only on page entry; image-handle clones are cheap shared handles, while title strings are owned by spawned scenes. The longer docs explain a real scroll/combobox clipping constraint. No runtime allocation or SIMD change warranted.
- `crates/harmonicon-menu/src/menu/pages/mod.rs` — module declarations only; no runtime work or API change needed.
- `crates/harmonicon-menu/src/menu/pages/artist_list.rs` — sort borrowed `(artist, songs)` entries so each artist's count is read directly instead of a second map lookup. The sorted vector and owned artist captured by each button are required by setup/UI ownership; no per-frame work or SIMD target. All 37 dev menu tests and workspace Clippy passed.
- `crates/harmonicon-menu/src/menu/pages/calibration.rs` — fix faded hit markers respawning: track the number spawned per recording generation rather than counting still-live markers, with an ECS regression test. Reuse the offset vector on reset; keep private clock/pitch-edge ticks out of Bevy change detection while retaining beat/hit changes; guard unchanged beat-dot, mic-bar, text, and visibility writes. The eight-hit average is too small for SIMD. Trimmed stale ASCII layout documentation. All 38 dev menu tests and workspace Clippy passed.
- `crates/harmonicon-menu/src/menu/pages/credits.rs` — remove the frame-delta reciprocal from the decorative harmonica bob, making its position a simple function of elapsed time at every frame rate. Markdown parsing and UI entities are built once on entry; render-layer propagation reuses a local traversal vector. Trimmed redundant module description. All 38 dev menu tests and workspace Clippy passed.
- `crates/harmonicon-menu/src/menu/pages/harp_check.rs` — refresh cost/chart-harp readouts on locale changes, reuse label storage, and avoid cloning/reinserting a source chart when its track is already selected. Cost remapping runs only when a choice changes; its small output strings and picker option vectors are owned by UI APIs. All 38 dev menu tests and workspace Clippy passed.
- `crates/harmonicon-menu/src/menu/pages/harp_check/tests.rs` — reviewed chart/harp matching, remap cost, choice seeding, and added-technique reporting fixtures. Test-only vectors and JSON fixtures have no production performance impact. No code change needed.
- `crates/harmonicon-menu/src/menu/pages/help_about.rs` — documentation lookup and OS launch happen only on a button press; About/Help UI is built once per page entry. Path candidates and localized strings are small owned setup values. No temporary frame vector, reference change, or SIMD target; no code change needed.
- Workspace Clippy (`--release --features dev --all-targets -D warnings`) had drifted during this review: `op_ref` in five Jam label comparisons, a collapsible `if` in the editor's timeline overlay, and needless borrows in the menu's harp check. All fixed; the workspace is clean again.

## Findings and follow-up

- `contributing/src/plugin-architecture.md` still contains stale Bevy 0.19 and `main.rs` wiring descriptions. A full rewrite should follow the actual current plugin registration; this is not yet marked complete.
- DSP already uses `rustfft`, which can select SIMD implementations for FFTs. YIN/MPM difference loops and NMF dot products may benefit from explicit SIMD or `simsimd`, but require representative recordings and benchmarks before changing floating-point reduction order or adding a dependency. NMF also allocates its output spectrum and activation vectors each call; changing that without affecting the public return type needs a careful buffer-reuse design.
- The calibration page still has hard-coded English status and button labels despite its localized setup text. Localization is a separate user-visible follow-up.
- Undo history's byte budget currently counts only note and tempo vector capacities; meter changes, annotations, expression intensities, and custom harp layouts are undercounted. A complete retained-size estimate needs their nested allocations included before relying on the 32 MiB cap as a strict limit.
- Lesson chart loading parses JSON directly before `load_harpchart`, unlike normal song loading, which runs `validated_harpchart`; a focused validation fix should reject unsupported chart features consistently without changing lesson save behavior.
- `harmonicon-editor`, `harmonicon-jam`, `harmonicon-bench`, and `harmonicon-gameplay` Rust sources are reviewed. Reviewing `harmonicon-menu`; next file: `src/menu/pages/jam_generate.rs`.
- Note-tail animation time lives in each note's own material (`params.z`), so `animate_note_tails`/`animate_note_tails_3d` `iter_mut` every tail material — plus the technique legend's — every frame, re-extracting and re-uploading each one. Moving the time into one shared uniform (a gameplay-clock value, not `globals.time`, since tails must freeze on pause and under reduced motion) would make tail uploads happen only on hit/miss/hold changes. Needs shader edits in `note_tail_2d.wesl`/`note_tail_3d.wesl` and on-screen verification.
- Run workspace Clippy after each crate, not only the crate's own: the lints above slipped in because only per-crate tests ran.
